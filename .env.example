# URL transformation settings for local development
# Set these values to transform specific hostnames to local addresses
# Leave TRANSFORM_SOURCE_HOSTNAME empty to disable URL transformation

# The hostname to transform (e.g., your external domain)
TRANSFORM_SOURCE_HOSTNAME=bucket.example.com

# Target hostname to transform to (default: localhost)
TRANSFORM_TARGET_HOSTNAME=localhost

# Target port for the transformed URL
TRANSFORM_TARGET_PORT=3003

# Target protocol for the transformed URL (default: http:)
TRANSFORM_TARGET_PROTOCOL=http:

# Optional: Your Gemini API key (if not provided via Authorization header)
# GEMINI_API_KEY=your_api_key_here
